version: 1.0
runtime: nodejs22

build:
  commands:
    build:
      - npm install

run:
  runtime-version: 22
  command: node server.js
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: "production"
