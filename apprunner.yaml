version: 1.0
runtime: nodejs22

build:
  commands:
    build:
      - npm install
      - npm run build

run:
  runtime-version: 22
  command: node server.js
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: "production"
