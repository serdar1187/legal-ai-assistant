version: 1.0
runtime: docker
build:
  commands:
    build:
      - echo "Building with Docker"
run:
  runtime-version: latest
  command: npm start
  network:
    port: 8080
    env: PORT
  env:
    - name: PORT
      value: "8080"
