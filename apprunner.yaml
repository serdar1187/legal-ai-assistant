version: 1.0
runtime: nodejs20

build:
  commands:
    pre-build:
      - npm ci --omit=dev
    build:
      - echo "no build step"

run:
  runtime-version: 20
  command: node server.js
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: "production"
